nav(id="menu_bar")
  .update_link
    a(href="/new")
      span(class="nav_item") 投稿する
    if user
      img(src=user.avatar_path class="avatar")
      a(href="/logout")
        span(class="nav_item") #{user.username}
    else
      a(href="/oauth/twitter")
        span(class="nav_item") ログイン
  a#menu_bar_logo(href="/") SmartMemo
